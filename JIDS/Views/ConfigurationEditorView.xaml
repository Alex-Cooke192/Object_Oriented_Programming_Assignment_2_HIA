<Page x:Class="JIDS.Views.ConfigurationEditorView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      MinWidth="600" MinHeight="500"
      Title="Configuration Editor">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header / Basic properties -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8" VerticalAlignment="Center">
            <StackPanel Orientation="Vertical" Width="360">
                <TextBlock Text="Name" FontWeight="SemiBold"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,8"/>
                <TextBlock Text="Cabin Dimensions" FontWeight="SemiBold"/>
                <TextBox Text="{Binding CabinDimensions, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,8"/>
                <TextBlock Text="Seating Capacity" FontWeight="SemiBold"/>
                <TextBox Text="{Binding SeatingCapacity, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,8"/>
            </StackPanel>

            <StackPanel Orientation="Vertical" Margin="16,0,0,0" VerticalAlignment="Top">
                <Button Content="Add Component" Width="140" Margin="0,0,0,8" Command="{Binding AddComponentCommand}"/>
                <Button Content="Remove Selected" Width="140" Margin="0,0,0,8" Command="{Binding RemoveComponentCommand}" />
                <Button Content="Clone Configuration" Width="140" Margin="0,0,0,8" Command="{Binding CloneCommand}" />
            </StackPanel>
        </StackPanel>

        <!-- Components DataGrid -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding InteriorComponents}"
                  SelectedItem="{Binding SelectedComponent, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Margin="0,0,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="2*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Tier" Binding="{Binding Tier, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Material" Binding="{Binding Material, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Position" Binding="{Binding Position, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="CreatedAt" Binding="{Binding CreatedAt, StringFormat={}{0:yyyy-MM-dd}}" Width="140"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer: Save / Cancel -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Margin="0,0,12,0" Foreground="DarkRed"/>
            <Button Content="Cancel" Width="100" Margin="0,0,8,0" Command="{Binding CancelCommand}"/>
            <Button Content="Save" Width="120" Command="{Binding SaveCommand}"/>
        </StackPanel>
    </Grid>
</Page>