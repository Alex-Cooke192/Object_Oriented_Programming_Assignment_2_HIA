<Window x:Class="JetInteriorApp.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="350" Width="400">
    <Grid>
        <!-- Username Label and TextBox -->
        <Label Content="Username" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="100,30,0,0"/>
        <TextBox Name="UsernameBox" Text="{Binding Username}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="200" Height="25" Margin="100,50,0,0"/>

        <!-- Email Label and TextBox (Always visible with note) -->
        <Label Content="Email (Only needed for registration)" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="100,90,0,0" Foreground="Gray"/>
        <TextBox Name="EmailBox" Text="{Binding Email}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="200" Height="25" Margin="100,110,0,0"/>

        <!-- Password Label and PasswordBox -->
        <Label Content="Password" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="100,150,0,0"/>
        <PasswordBox Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" HorizontalAlignment="Left" VerticalAlignment="Top" Width="200" Height="25" Margin="100,170,0,0"/>

        <!-- Register Button -->
        <Button Content="Register" Command="{Binding RegisterCommand}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="200" Height="30" Margin="100,210,0,0"/>

        <!-- Login Button -->
        <Button Content="Login" Command="{Binding LoginCommand}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="200" Height="30" Margin="100,250,0,0"/>

        <!-- Status Message -->
        <TextBlock Text="{Binding StatusMessage}" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="5"/>
    </Grid>
</Window>


