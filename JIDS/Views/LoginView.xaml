<Window x:Class="JetInteriorApp.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login/Register" Height="350" Width="300">

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="240" >

            <!-- Username -->
            <StackPanel Margin="5">
                <Label Content="Username:" />
                <TextBox Width="220"
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <!-- Email -->
            <StackPanel Margin="5">
                <Label Content="Email (Registration Only):" />
                <TextBox Width="220"
                         Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <!-- Password -->
            <StackPanel Margin="5">
                <Label Content="Password:" />
                <PasswordBox x:Name="PasswordBox" Width="220"
                             PasswordChanged="PasswordBox_PasswordChanged"/>
            </StackPanel>

            <!-- Buttons -->
            <Button Content="Login" Width="240" Margin="5" Command="{Binding LoginCommand}" />
            <Button Content="Register" Width="240" Margin="5" Command="{Binding RegisterCommand}" />

            <!-- Status Message -->
            <TextBlock Text="{Binding StatusMessage}" Margin="5" Foreground="Red" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Window>

