<Window x:Class="YourApp.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="200" Width="300">
    <Grid Margin="10">
        <StackPanel>
            <TextBox PlaceholderText="Username" Text="{Binding Username}" Margin="0,0,0,10"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,10"/>
            <Button Content="Login" Command="{Binding LoginCommand}" Click="OnLoginClick"/>
            <TextBlock Text="{Binding StatusMessage}" Foreground="Red" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>