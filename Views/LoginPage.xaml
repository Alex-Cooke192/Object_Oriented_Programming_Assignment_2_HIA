<Window x:Class="YourApp.Views.LoginPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:YourApp.ViewModels"
        Title="Login" Height="250" Width="400">
    <Window.DataContext>
        <vm:LoginViewModel />
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Margin="0 0 0 10" 
                 PlaceholderText="Username"
                 Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Margin="0 0 0 10" />

        <Button Grid.Row="2" Content="Login" Width="100" HorizontalAlignment="Left"
                Command="{Binding LoginCommand}"
                Click="LoginButton_Click" />

        <TextBlock Grid.Row="3" Margin="0 10 0 0" Text="{Binding StatusMessage}" />
    </Grid>
</Window>